import React, { useRef } from "react";
import Marks from "./marks";

const TimeLine = () => {
  return (
    <div id="timeLine">
      <Marks />
    </div>
  );
};

export default TimeLine;
